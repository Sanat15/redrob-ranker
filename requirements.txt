# Redrob Hackathon — Python dependencies
#
# Core ranking pipeline (scorer.py, rank.py) uses ONLY the standard library.
# No external packages are needed for the actual ranking — this satisfies the
# "no network during ranking" constraint trivially.
#
# The two packages below are for the Streamlit sandbox (app.py) only.

streamlit>=1.28.0
pandas>=2.0.0
